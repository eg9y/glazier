pre-commit:
  parallel: true
  commands:
    lint:
      run: pnpm biome check --write --unsafe --staged --no-errors-on-unmatched && git add -u
    typecheck:
      run: pnpm --filter glazier tsc
    build:
      run: pnpm build:lib
    test:
      run: pnpm --filter glazier test:ci
